name: delulu
description: A premium social discovery app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dotted_border: ^3.1.0
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  image_picker: ^0.8.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true